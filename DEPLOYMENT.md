# Neon Pulse Collector Deployment

Single supported flow (non-interactive):

```bash
npm run deploy
```

This command compiles the Payment contract and deploys it to TON testnet using `@ton/ton`.
See `contracts/DEPLOYMENT.md` for environment setup and details.
